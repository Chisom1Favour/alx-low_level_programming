#include "main.h"
/**
 * add - Adds two integers and returns the result
 *
 * @x: First number.
 * @y: Second number
 *
 * Return: result of additon of first and second number
 *
 */
int add(int x, int y)
{
	return (x + y);
}
