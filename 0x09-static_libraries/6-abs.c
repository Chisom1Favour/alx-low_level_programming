#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @x: input number as integer
 *
 * Return: absolute value
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (x * -1);
	}

}


