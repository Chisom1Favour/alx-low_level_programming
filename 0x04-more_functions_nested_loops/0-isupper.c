#include "main.h"
#include <stdio.h>
/**
 * main - function that checks for uppercase character
 *
 * Return: 1 if c is uppercase
 *
 * Return: 0 if otherwise
 *
 */
int main(void)
{
	char c;

	if (c >='A' && c <= 'Z')
	{
		putchar('c');
	}
	return 1
	else
	{
		putchar('c');
	}
	return 0
}
